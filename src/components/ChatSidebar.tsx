// React functional component that renders a coloured div to test the ChatSidebar component

import React from 'react';

export const ChatSidebar = () => {
  return <div style={{ backgroundColor: 'red', height: '100vh', width: '20%' }} />;
};